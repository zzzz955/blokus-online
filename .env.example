# Blokus Online Configuration
# 이 파일을 .env로 복사해서 사용하세요

# ========== Docker Image Configuration ==========
# 배포 시 사용할 이미지 태그 (기본값: latest)
IMAGE_TAG=latest
GAME_SERVER_IMAGE=ghcr.io/zzzz955/blokus-online/blokus-game-server:${IMAGE_TAG:-latest}
WEB_SERVER_IMAGE=ghcr.io/zzzz955/blokus-online/blokus-web-server:${IMAGE_TAG:-latest}

# ========== Client Configuration ==========
# 클라이언트 서버 연결 설정
BLOKUS_SERVER_HOST=localhost
BLOKUS_SERVER_PORT=9999

# 배포 환경에서는 다음과 같이 설정:
# BLOKUS_SERVER_HOST=blokus-online.mooo.com
# BLOKUS_SERVER_PORT=9999